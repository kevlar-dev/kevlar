addons:
    apt:
        packages:
            - libssl-dev
language: python
python:
    - 2.7
    - 3.5
install:
    - pip install --upgrade pip setuptools pytest pep8
    - pip install git+https://github.com/standage/pysam.git
    - pip install git+https://github.com/dib-lab/khmer.git
    - pip install .
script:
    - make test
    - make style
