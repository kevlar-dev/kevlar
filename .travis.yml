addons:
    apt:
        packages:
            - curl
            - libcurl3-dev
language: python
python:
    - 2.7
    - 3.5
install:
    - pip install --upgrade pip setuptools pytest
    - make devenv
    - pip install .
script:
    - make test
    - make style
